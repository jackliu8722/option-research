# 公式渲染自测（用完可删）

> 请在 **github.com 网页**打开本文件，告诉我**下面哪几号没正常渲染**（显示成原始 ` $...$ ` 文字、或空白、或乱码）。据此精确定位失败构造。

**1. 行内·最简**：质能方程 $E=mc^2$ 应显示为公式。

**2. 行内·下标**： $S_0 = 100$ 。

**3. 行内·千分位**： $S_0 = 61{,}672$ （这里用了 `{,}`）。

**4. 行内·千分位(逗号直接写)**： $S_0 = 61,672$ 。

**5. 行内·希腊字母**： $\Delta = 0.5,\ \sigma = 0.8$ 。

**6. 行内·\text**： $\text{Max loss} = 100$ 。

**7. 行内·百分号**： $\sigma = 50\%$ 。

**8. 行内·分数根号**： $d_1 = \frac{a}{b\sqrt{\tau}}$ 。

**9. 一行里多个行内公式**： $a=1$ 和 $b=2$ 以及 $c=3$ 。

**10. 块级·最简**（单行 `$$`）：

$$ E = mc^2 $$

**11. 块级·复杂**（单行 `$$`）：

$$ d_1 = \frac{\ln(S/K) + (r + \tfrac{1}{2}\sigma^2)\tau}{\sigma\sqrt{\tau}} $$

**12. 块级·千分位**：

$$ 60{,}000 \times 0.80 \times \sqrt{1/365} \approx 2{,}513 $$

**13. 块级·多行 `$$`（规范写法）**：

$$
E = mc^2
$$

**14. 表格内行内公式**：

| 项 | 值 |
|---|---|
| Delta | $\Delta = 0.5$ |
| 千分位 | $S_0 = 61{,}672$ |

**15. 中文紧贴行内公式**：盈亏平衡 $S_T = K + p$ 就是这样。
